<div class="container">
  <h1 class="text-center"><%= @cocktail.name %></h1>
  <%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill, alt: "cocktail" %>
  <%= link_to new_cocktail_dose_path(@cocktail) do %>
    <h2> <i class="fas fa-plus"> </i>Add a dose</h2>
  <% end %>
  <p>
  <%= link_to 'Return to cocktails', cocktails_path %>
  </p>
</div>
